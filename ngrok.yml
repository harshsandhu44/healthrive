version: "2"
authtoken: ${NGROK_AUTHTOKEN}
tunnels:
  healthrive:
    addr: healthrive-dev:3000
    proto: http
    hostname: ${NGROK_DOMAIN}
    schemes:
      - https
    host_header: localhost:3000
web_addr: 0.0.0.0:4040
log_level: info
log_format: term